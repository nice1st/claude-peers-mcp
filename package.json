{
  "name": "claude-peers-broker",
  "version": "1.0.3",
  "description": "Broker for claude-peers",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "broker": "bun broker.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
